import PlaceholderPage from "@/components/PlaceholderPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Pick Guards — Heal The World Collection",
};

export default function PickGuardsPage() {
    return <PlaceholderPage title="Pick Guards" category="pick guard" />;
}
