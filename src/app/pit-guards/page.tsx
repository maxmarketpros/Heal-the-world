import PlaceholderPage from "@/components/PlaceholderPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Pit Guards — Heal The World Collection",
};

export default function PitGuardsPage() {
    return <PlaceholderPage title="Pit Guards" category="pit guard" />;
}
