import PlaceholderPage from "@/components/PlaceholderPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Backstage Passes — Heal The World Collection",
};

export default function BackstagePassesPage() {
    return <PlaceholderPage title="Backstage Passes" category="backstage pass" />;
}
